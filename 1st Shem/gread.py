def gread():
    #create a list to store strings from the user
    l=[]
    cources=[]
    #define a loop to read user's input
    while True:
        str=input()
        if(str=='Endofinput'):
            break
        else:
            l.append(str)
